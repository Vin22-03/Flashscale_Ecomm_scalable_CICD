apiVersion: v1
kind: Service
metadata:
  name: flashscale-frontend
  namespace: flashscale
spec:
  type: LoadBalancer
  selector:
    app: flashscale-frontend
    color: __ACTIVE_COLOR__
  ports:
    - port: 80
      targetPort: 80
